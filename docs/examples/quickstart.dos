// Choose a connection in the DOS toolbar before running.
// This example creates only session variables and an in-memory table.
def addOne(value) {
    return value + 1
}
alpha = 1
beta = 2
beta = 3
answer = addOne(alpha)
prices = table(1..3 as id, 10.5 11.2 12.3 as price)
print(answer)
select * from prices where price > 11
